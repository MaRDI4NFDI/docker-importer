name: docker-importer

build: null
push: null

deployments:
  - name: staging
    entrypoint: prefect_workflows/mardi_importer.py:mardi_importer_flow
    work_pool:
      name: staging-k8s-pool
    pull:
      - prefect.deployments.steps.git_clone:
          repository: https://github.com/MaRDI4NFDI/docker-importer.git
          branch: staging
